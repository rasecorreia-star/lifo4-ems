# ==========================================================
# LIFO4 EMS - Full Stack Environment Variables
# Copy this file to .env.development, .env.staging, .env.production
# ==========================================================

# ==========================================================
# FRONTEND VARIABLES (apps/frontend)
# ==========================================================

# API Configuration
VITE_API_URL=http://localhost:3001
VITE_API_VERSION=v1

# WebSocket Configuration
VITE_WS_URL=ws://localhost:3001

# App Configuration
VITE_APP_NAME=Lifo4 EMS
VITE_APP_VERSION=1.0.0

# Demo Mode (CRITICAL: Must be false in production!)
# When DEMO_MODE=true, requires VITE_DEMO_EMAIL and VITE_DEMO_PASSWORD
# validateDemoMode() in apps/frontend/src/services/config.ts enforces this
VITE_DEMO_MODE=false
VITE_DEMO_EMAIL=demo@lifo4.com.br
VITE_DEMO_PASSWORD=demo123

# Feature Flags
VITE_ENABLE_PWA=false
VITE_ENABLE_ANALYTICS=false
VITE_ENABLE_SENTRY=false

# Error Tracking (Sentry)
VITE_SENTRY_DSN=

# Firebase (Optional - for auth integration)
VITE_FIREBASE_API_KEY=your-firebase-api-key
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your-sender-id
VITE_FIREBASE_APP_ID=your-app-id

# ==========================================================
# BACKEND VARIABLES (apps/backend) — Not yet in place
# ==========================================================

# Database Configuration
# DATABASE_URL=postgresql://user:password@localhost:5432/emsdb

# JWT Configuration
# JWT_SECRET=your-secret-key-change-in-production-minimum-32-chars
# JWT_EXPIRATION=24h

# CORS Configuration
# CORS_ORIGIN=http://localhost:3000

# Server Configuration
# PORT=3001
# NODE_ENV=development
# LOG_LEVEL=info

# Optional: Redis Cache
# REDIS_URL=redis://localhost:6379

# Optional: Error Tracking (Sentry)
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# ==========================================================
# DEVELOPMENT NOTES
# ==========================================================

# PHASE 1 SECURITY ENFORCEMENTS:
# 1. config.ts validateEnv() throws error if VITE_API_URL or VITE_WS_URL missing
# 2. config.ts validateDemoMode() throws error if DEMO_MODE=true but credentials missing
# 3. GitHub Actions grep checks entire codebase for hardcoded credentials
# 4. Never commit actual credentials — use .env.local for local overrides

# SETUP INSTRUCTIONS:
# 1. Development:
#    $ cp .env.example .env.development
#    $ npm run dev
#
# 2. Production:
#    $ cp .env.example .env.production
#    $ Edit with real values
#    $ npm run build
#    $ npm start

# DEMO MODE SECURITY:
# - Demo credentials (demo@lifo4.com.br / demo123) are PLACEHOLDERS
# - Update VITE_DEMO_EMAIL and VITE_DEMO_PASSWORD to real test account in .env files
# - In production, keep VITE_DEMO_MODE=false
# - Never hardcode credentials in source files
