# EMS BESS - Demo Backend (Node.js puro, sem TypeScript)
FROM node:20-alpine

WORKDIR /app

# Install apenas express, cors, jwt
RUN npm init -y && npm install express cors jsonwebtoken

# Copy demo server
COPY demo-server-full.js ./server.js

# Expose port
EXPOSE 3000

# Health check
HEALTHCHECK --interval=30s --timeout=10s --start-period=5s --retries=3 \
    CMD wget -q --spider http://localhost:3000/health || exit 1

CMD ["node", "server.js"]
